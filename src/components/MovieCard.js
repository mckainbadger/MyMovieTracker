//** Import Statements

//** Setup (define helper functions and variables here)

const MovieCard = (props) => {
    //** Destructure Props
    const {
  
    } = props
  
    //** State Variables
  
    //** Component Logic
  
    //** Return JSX
    return (
      <div> 
        Movie Card
      </div>
    )
  }
  export default MovieCard